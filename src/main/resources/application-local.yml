spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/minor_app?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Bogota
    username: minor_remoto
    password: qigxu8-niwqAv-wyskax
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        "[dialect.storage_engine]": innodb

security-core:
  secret: MiClaveSecretaLocal123456789012345
  public-paths:
    - /auth/login